<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dinokeylas.melijoonline">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@drawable/melijo_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/melijo_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".VegetablePackageActivity"/>
        <activity android:name=".FruitActivity" />
        <activity android:name=".MeatActivity" />
        <activity android:name=".SeaFoodActivity" />
        <activity android:name=".DetailItemActivity" />
        <activity android:name=".view.SeasoningActivity" />
        <activity android:name=".EditProfileActivity" />
        <activity android:name=".view.VegetableActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity" />
        <activity android:name=".view.RegisterActivity" />
        <activity android:name=".view.LoginActivity" />
    </application>

</manifest>